name: Deploy Design Lab to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "launch.html"
      - "Design/**"
      - "Insight/knowledge-map-whitepaper/**"
      - "vendor/pto-design-system/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Prepare static site
        shell: bash
        run: |
          mkdir -p _site/vendor _site/Insight
          cp launch.html _site/index.html
          cp launch.html _site/launch.html
          cp -R Design _site/Design
          cp -R Insight/knowledge-map-whitepaper _site/Insight/knowledge-map-whitepaper
          cp -R vendor/pto-design-system _site/vendor/pto-design-system
          touch _site/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
